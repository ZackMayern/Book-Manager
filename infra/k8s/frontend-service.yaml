apiVersion: v1
kind: Service
metadata:
  name: book-manager-ui-service
  namespace: book-manager
spec:
  selector:
    app: book-manager-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer